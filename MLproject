name: autoML

conda_env: conda.yml

entry_points:
  main:
    parameters:
      alpha: {type: float, default: 0.5}
      best_quality: {type: string, enum: ["best_quality", "medium_quality"], default: best_quality}
    command: "python autogluon-mlflow-project.py {alpha} {best_quality}"
